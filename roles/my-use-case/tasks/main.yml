##

# Partner PowerUp BizObs - ACE-BOX Role Tasks (Optimized)# Partner PowerUp BizObs - ACE-BOX Use Case (Ultra-Simple Fixed Version)

# Uses ACE-BOX curated roles and optimized startup script# Fixes APT lock issues by removing manual Node.js installation

## 

---

---- debug:

- debug:    msg: "🚀 Starting Ultra-Simple Partner PowerUp BizObs deployment..."

    msg: "🚀 Starting Partner PowerUp BizObs deployment..."

# Store role path for file references

# Set application variables- set_fact:

- set_fact:    role_path_abs: "{{ role_path }}"

    bizobs_repo_url: "https://github.com/lawrobar90/Partner-PowerUp-BizObs-App.git"

    bizobs_app_dir: "/home/{{ ace_box_user | default('dt_training') }}/Partner-PowerUp-BizObs-App"# Set application variables

    bizobs_domain: "bizobs.{{ ingress_domain }}"- set_fact:

    bizobs_repo_url: "https://github.com/lawrobar90/Partner-PowerUp-BizObs-App.git"

# Store role path for file references    bizobs_app_dir: "/home/{{ ace_box_user | default('dt_training') }}/Partner-PowerUp-BizObs-App"

- set_fact:    bizobs_domain: "bizobs.{{ ingress_domain }}"

    role_path_abs: "{{ role_path }}"

#

# # STEP 1: Authentication & Dynatrace Setup

# STEP 1: Dynatrace Authentication (ACE-BOX curated role)#

#- include_role:

- include_role:    name: dt-access-token

    name: dt-access-token  vars:

  vars:    access_token_var_name: "monaco_cleanup_api_token"

    access_token_var_name: "monaco_cleanup_api_token"    access_token_scope:

    access_token_scope:      [

      [        "slo.read", "slo.write", "CaptureRequestData", "credentialVault.read", 

        "slo.read", "slo.write", "CaptureRequestData", "credentialVault.read",         "credentialVault.write", "DataExport", "DataPrivacy", "ExternalSyntheticIntegration",

        "credentialVault.write", "DataExport", "DataPrivacy", "ExternalSyntheticIntegration",        "ReadConfig", "WriteConfig", "events.ingest", "settings.read", "settings.write",

        "ReadConfig", "WriteConfig", "events.ingest", "settings.read", "settings.write",        "metrics.ingest", "openTelemetryTrace.ingest", "logs.ingest"

        "metrics.ingest", "openTelemetryTrace.ingest", "logs.ingest"      ]

      ]

#

## STEP 2: Infrastructure Setup (ACE-BOX curated roles handle Node.js!)

# STEP 2: Core Infrastructure (ACE-BOX curated roles)#

#- include_role:

- include_role:    name: "microk8s"

    name: "microk8s"

- include_role:

- include_role:    name: "nodejs"

    name: "nodejs"

# 

# # STEP 3: Application Deployment (No manual apt installs!)

# STEP 3: Application Deployment (Streamlined - NO APT CONFLICTS)#

#- name: Clone Partner PowerUp BizObs repository

- name: Clone Partner PowerUp BizObs repository  ansible.builtin.git:

  ansible.builtin.git:    repo: "{{ bizobs_repo_url }}"

    repo: "{{ bizobs_repo_url }}"    dest: "{{ bizobs_app_dir }}"

    dest: "{{ bizobs_app_dir }}"    version: "main"

    version: "main"    force: yes

    force: yes  become_user: "{{ ace_box_user | default('dt_training') }}"

  become_user: "{{ ace_box_user | default('dt_training') }}"

- name: Install application dependencies

- name: Install application dependencies  ansible.builtin.command:

  ansible.builtin.command:    cmd: npm install --production

    cmd: npm install --production    chdir: "{{ bizobs_app_dir }}"

    chdir: "{{ bizobs_app_dir }}"  become_user: "{{ ace_box_user | default('dt_training') }}"

  become_user: "{{ ace_box_user | default('dt_training') }}"

  register: npm_install_result- name: Copy ACE-BOX optimized startup script

  ansible.builtin.copy:

- name: Display npm install result    src: "{{ role_path_abs }}/../../start-server-ace-optimized.sh"

  debug:    dest: "{{ bizobs_app_dir }}/start-server.sh"

    msg: "NPM install completed: {{ npm_install_result.stdout_lines | join(' ') }}"    mode: '0755'



- name: Copy ACE-BOX optimized startup script (ENSURES OUR SCRIPT IS USED)- name: Start BizObs application (ACE-BOX optimized)

  ansible.builtin.copy:  ansible.builtin.shell: /bin/sh ./start-server.sh

    src: "{{ role_path_abs }}/../../start-server-ace-optimized.sh"  become: true

    dest: "{{ bizobs_app_dir }}/start-server.sh"  args:

    mode: '0755'    chdir: "{{ bizobs_app_dir }}"

    force: yes  async: 30

  poll: 0

- name: Verify Node.js installation

  ansible.builtin.command: node --version- name: Wait for application startup

  register: node_version  ansible.builtin.wait_for:

  become_user: "{{ ace_box_user | default('dt_training') }}"    port: 8080

    host: localhost

- name: Display Node.js version    delay: 5

  debug:    timeout: 45

    msg: "Node.js version: {{ node_version.stdout }}"

#

- name: Start BizObs application (ACE-BOX optimized script)# STEP 4: Observability & Monitoring

  ansible.builtin.shell: /bin/sh ./start-server.sh#

  become: true- include_role:

  args:    name: "dt-oneagent-classic"

    chdir: "{{ bizobs_app_dir }}"

  async: 30- include_role:

  poll: 0    name: "monaco-v2"

  register: app_start_result

- include_role:

- name: Display application startup    name: monaco-v2

  debug:    tasks_from: apply-monaco

    msg: "BizObs application starting in background..."    apply:

      environment:

- name: Wait for application startup        DT_API_TOKEN: "{{ dynatrace_api_token }}"

  ansible.builtin.wait_for:        DT_PLATFORM_TENANT_URL: "{{ extra_vars.dt_environment_url_gen3.rstrip('/') if extra_vars.dt_environment_url_gen3 is defined else '' }}"

    port: 8080        DT_OAUTH_CLIENT_ID: "{{ extra_vars.dt_oauth_client_id | default('') }}"

    host: localhost        DT_OAUTH_CLIENT_SECRET: "{{ extra_vars.dt_oauth_client_secret | default('') }}"

    delay: 5        DT_OAUTH_SSO_ENDPOINT: "{{ extra_vars.dt_oauth_sso_endpoint | default('') }}"

    timeout: 45        INGRESS_DOMAIN: "{{ ingress_domain }}"

  register: port_ready  vars:

    monaco_projects_root: "{{ role_path_abs }}/files/monaco"

- name: Verify application is responding    monaco_project: ""

  ansible.builtin.uri:    monaco_manifest_path: "{{ role_path_abs }}/files/monaco/manifest.yml"

    url: "http://localhost:8080/health"

    method: GET#

    status_code: 200# STEP 5: Team Collaboration

  register: health_check_startup#

  retries: 3- include_role:

  delay: 5    name: "mattermost"

  until: health_check_startup.status == 200

#

## STEP 6: Dashboard Setup

# STEP 4: Observability & Monitoring (ACE-BOX curated roles)#

#- name: Configure Dashboard with BizObs links

- include_role:  block:

    name: "dt-oneagent-classic"    - set_fact:

        include_dashboard_value_file: "{{ role_path_abs }}/templates/bizobs-dashboard.yml.j2"

- include_role:

    name: "monaco-v2"    - include_role:

        name: dashboard

- name: Deploy Monaco configuration        tasks_from: template-values-file

  include_role:

    name: monaco-v2    - include_role:

    tasks_from: apply-monaco        name: "dashboard"

    apply:

      environment:#

        DT_API_TOKEN: "{{ dynatrace_api_token }}"# STEP 7: External Access (Kubernetes ingress)

        DT_PLATFORM_TENANT_URL: "{{ extra_vars.dt_environment_url_gen3.rstrip('/') if extra_vars.dt_environment_url_gen3 is defined else '' }}"#

        DT_OAUTH_CLIENT_ID: "{{ extra_vars.dt_oauth_client_id | default('') }}"- name: Create BizObs Kubernetes service

        DT_OAUTH_CLIENT_SECRET: "{{ extra_vars.dt_oauth_client_secret | default('') }}"  kubernetes.core.k8s:

        DT_OAUTH_SSO_ENDPOINT: "{{ extra_vars.dt_oauth_sso_endpoint | default('') }}"    definition:

        INGRESS_DOMAIN: "{{ ingress_domain }}"      apiVersion: v1

  vars:      kind: Service

    monaco_projects_root: "{{ role_path_abs }}/files/monaco"      metadata:

    monaco_project: ""        name: bizobs-service

    monaco_manifest_path: "{{ role_path_abs }}/files/monaco/manifest.yml"        namespace: default

        labels:

#          app: bizobs

# STEP 5: Team Collaboration (ACE-BOX curated roles)          component: partner-powerup

#      spec:

- include_role:        type: ExternalName

    name: "mattermost"        externalName: localhost

        ports:

- name: Configure Dashboard with BizObs links        - port: 8080

  block:          targetPort: 8080

    - set_fact:          name: http

        include_dashboard_value_file: "{{ role_path_abs }}/templates/bizobs-dashboard.yml.j2"

- name: Create BizObs ingress for external access

    - include_role:  kubernetes.core.k8s:

        name: dashboard    definition:

        tasks_from: template-values-file      apiVersion: networking.k8s.io/v1

      kind: Ingress

    - include_role:      metadata:

        name: "dashboard"        name: bizobs-ingress

        namespace: default

#        labels:

# STEP 6: External Access (Kubernetes ingress)          app: bizobs

#          component: partner-powerup

- name: Create BizObs Kubernetes service        annotations:

  kubernetes.core.k8s:          nginx.ingress.kubernetes.io/rewrite-target: /

    definition:          nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"

      apiVersion: v1          nginx.ingress.kubernetes.io/proxy-read-timeout: "300"

      kind: Service      spec:

      metadata:        ingressClassName: "{{ ingress_class | default('nginx') }}"

        name: bizobs-service        rules:

        namespace: default        - host: "{{ bizobs_domain }}"

        labels:          http:

          app: bizobs            paths:

          component: partner-powerup            - path: /

      spec:              pathType: Prefix

        type: ExternalName              backend:

        externalName: localhost                service:

        ports:                  name: bizobs-service

        - port: 8080                  port:

          targetPort: 8080                    number: 8080

          name: http

# Final verification and success message

- name: Create BizObs ingress for external access- name: Verify application health

  kubernetes.core.k8s:  ansible.builtin.uri:

    definition:    url: "http://localhost:8080/health"

      apiVersion: networking.k8s.io/v1    method: GET

      kind: Ingress    status_code: 200

      metadata:  register: health_check

        name: bizobs-ingress  retries: 3

        namespace: default  delay: 5

        labels:

          app: bizobs- debug:

          component: partner-powerup    msg: |

        annotations:      ✅ Partner PowerUp BizObs deployed successfully!

          nginx.ingress.kubernetes.io/rewrite-target: /      

          nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"      🔗 Access URLs:

          nginx.ingress.kubernetes.io/proxy-read-timeout: "300"      - BizObs App: {{ ingress_protocol | default('http') }}://{{ bizobs_domain }}/

      spec:      - Dashboard: {{ ingress_protocol | default('http') }}://dashboard.{{ ingress_domain }}/

        ingressClassName: "{{ ingress_class | default('nginx') }}"      - Mattermost: {{ ingress_protocol | default('http') }}://mattermost.{{ ingress_domain }}/

        rules:      

        - host: "{{ bizobs_domain }}"      🎯 Health Check: {{ health_check.status }} - {{ health_check.msg | default('OK') }}

          http:      

            paths:      🎭 Demo Features Ready:

            - path: /      - Customer Journey Simulation (Insurance, Retail, Tech, Banking)

              pathType: Prefix      - Multi-persona Load Generation (Karen, Raj, Alex, Sophia)

              backend:      - Dynatrace Integration with Business Events

                service:      - Real-time Observability Dashboard

                  name: bizobs-service      - Dynamic Service Mesh Creation

                  port:      

                    number: 8080      🚀 Fixed: No more APT lock issues!



# Final verification and success message# Store role path for file references

- name: Verify application health- set_fact:

  ansible.builtin.uri:    role_path_abs: "{{ role_path }}"

    url: "http://localhost:8080/health"

    method: GET# 

    status_code: 200# STEP 1: Authentication & Dynatrace Setup

  register: health_check# dt-access-token is a curated role for Dynatrace API access

  retries: 3# Required for Monaco configuration management

  delay: 5#

- include_role:

- debug:    name: dt-access-token

    msg: |  vars:

      ✅ Partner PowerUp BizObs deployed successfully via ACE-BOX!    access_token_var_name: "monaco_cleanup_api_token"

          access_token_scope:

      🔗 Access URLs:      [

      - BizObs App: {{ ingress_protocol | default('http') }}://{{ bizobs_domain }}/        "slo.read",

      - Dashboard: {{ ingress_protocol | default('http') }}://dashboard.{{ ingress_domain }}/        "slo.write", 

      - Mattermost: {{ ingress_protocol | default('http') }}://mattermost.{{ ingress_domain }}/        "CaptureRequestData",

              "credentialVault.read",

      🎯 Health Check: {{ health_check.status }} - {{ health_check.msg | default('OK') }}        "credentialVault.write",

              "DataExport",

      🎭 Demo Features Ready:        "DataPrivacy",

      - Customer Journey Simulation (Insurance, Retail, Tech, Banking)        "ExternalSyntheticIntegration",

      - Multi-persona Load Generation (Karen, Raj, Alex, Sophia)        "ReadConfig",

      - Dynatrace Integration with Business Events        "WriteConfig",

      - Real-time Observability Dashboard        "events.ingest",

      - Dynamic Service Mesh Creation        "settings.read",

              "settings.write",

      🚀 ACE-BOX deployment complete with optimized startup script!        "metrics.ingest",
        "openTelemetryTrace.ingest",
        "logs.ingest"
      ]

# Generate OAuth Token (exactly as in working implementation)
- block:
    - name: Request a DT OAuth Access Token
      ansible.builtin.uri:
        url: "{{ extra_vars.dt_oauth_sso_endpoint }}"
        method: POST
        headers:
          Content-Type: "application/x-www-form-urlencoded"
        body_format: form-urlencoded
        body:
          grant_type: "client_credentials"
          client_id: "{{ extra_vars.dt_oauth_client_id }}"
          client_secret: "{{ extra_vars.dt_oauth_client_secret }}"
          scope: "document:documents:read document:documents:write document:direct-shares:read document:direct-shares:write"
          resource: "{{ extra_vars.dt_oauth_account_urn }}"
      register: auth_response_raw
      failed_when: auth_response_raw.status != 200
      when: 
        - extra_vars.dt_oauth_client_id is defined
        - extra_vars.dt_oauth_client_secret is defined
        - extra_vars.dt_oauth_sso_endpoint is defined

    - name: Set OAuth Access Token
      set_fact:
        dt_oauth_access_token: "{{ auth_response_raw.json.access_token }}"
      when: auth_response_raw is defined and auth_response_raw.status == 200

# Install Biz Flow App using dt-platform role
- name: Install Biz Flow App
  ignore_errors: true
  include_role:
    name: dt-platform
    tasks_from: ensure-app
  vars:
    dt_app_id: "dynatrace.biz.flow"
    dt_app_version: "1.20.3"
    dt_environment_url_gen3: "{{ extra_vars.dt_environment_url_gen3 }}"
    dt_oauth_sso_endpoint: "{{ extra_vars.dt_oauth_sso_endpoint }}"
    dt_oauth_client_id: "{{ extra_vars.dt_oauth_client_id }}"
    dt_oauth_client_secret: "{{ extra_vars.dt_oauth_client_secret }}"
    dt_oauth_account_urn: "{{ extra_vars.dt_oauth_account_urn }}"
  when: 
    - extra_vars.dt_environment_url_gen3 is defined
    - extra_vars.dt_oauth_client_id is defined

#
# STEP 2: Infrastructure Setup
# Kubernetes infrastructure is required for Mattermost and Dashboard
#
- include_role:
    name: "microk8s"

#
# STEP 3: Monaco Configuration (requires auth tokens)
# Apply Monaco configuration for Dynatrace setup
#
- include_role:
    name: "monaco-v2"

- include_role:
    name: monaco-v2
    tasks_from: apply-monaco
    apply:
      environment:
        DT_API_TOKEN: "{{ dynatrace_api_token }}"
        DT_PLATFORM_TENANT_URL: "{{ extra_vars.dt_environment_url_gen3.rstrip('/') if extra_vars.dt_environment_url_gen3 is defined else '' }}"
        DT_OAUTH_CLIENT_ID: "{{ extra_vars.dt_oauth_client_id | default('') }}"
        DT_OAUTH_CLIENT_SECRET: "{{ extra_vars.dt_oauth_client_secret | default('') }}"
        DT_OAUTH_SSO_ENDPOINT: "{{ extra_vars.dt_oauth_sso_endpoint | default('') }}"
        INGRESS_DOMAIN: "{{ ingress_domain }}"
  vars:
    monaco_projects_root: "{{ role_path_abs }}/files/monaco"
    monaco_project: ""
    monaco_manifest_path: "{{ role_path_abs }}/files/monaco/manifest.yml"

#
# STEP 4: Mattermost (requires Kubernetes)
# Team collaboration platform
#
- include_role:
    name: "mattermost"

#
# STEP 5: OneAgent (after Kubernetes is ready)
# Dynatrace observability agent
#
- include_role:
    name: "dt-oneagent-classic"

#
# STEP 6: BizObs Application (runs on EC2, uses ingress for external access)
# This is the main Partner PowerUp BizObs application
#
- include_role:
    name: "app-partner-powerup-bizobs"

#
# STEP 7: Dashboard Setup (requires Kubernetes and BizObs to be running)
# Configure dashboard with links to all services
#
- name: Configure Dashboard with BizObs links
  block:
    - set_fact:
        include_dashboard_value_file: "{{ role_path_abs }}/templates/bizobs-dashboard.yml.j2"

    - include_role:
        name: dashboard
        tasks_from: template-values-file

    - include_role:
        name: "dashboard"

# Final status with all access URLs
- debug:
    msg: |
      🎉 Partner PowerUp BizObs deployed successfully!
      
      📊 DASHBOARD & COLLABORATION:
      - Dashboard: {{ ingress_protocol | default('http') }}://dashboard.{{ ingress_domain }}/
      - Mattermost: {{ ingress_protocol | default('http') }}://mattermost.{{ ingress_domain }}/
      
      🚀 BIZOBS APPLICATION:
      - Main App: {{ ingress_protocol | default('http') }}://bizobs.{{ ingress_domain }}/
      - Health Check: {{ ingress_protocol | default('http') }}://bizobs.{{ ingress_domain }}/health
      - Admin Panel: {{ ingress_protocol | default('http') }}://bizobs.{{ ingress_domain }}/api/admin/services/status
      - Journey Simulation: {{ ingress_protocol | default('http') }}://bizobs.{{ ingress_domain }}/api/journey-simulation/simulate-journey
      - Load Generation: {{ ingress_protocol | default('http') }}://bizobs.{{ ingress_domain }}/api/load-gen/start
      
      🔧 DYNATRACE INTEGRATION:
      - Tenant: {{ extra_vars.dt_environment_url_gen3 if extra_vars.dt_environment_url_gen3 is defined else 'Configured via environment' }}
      - OneAgent: Installed and monitoring
      - Biz Flow App: Installed
      - Monaco Config: Applied
      
      🎭 DEMO FEATURES READY:
      - Customer Journey Simulation (Insurance, Retail, Tech, Banking)
      - Multi-persona Load Generation (Karen, Raj, Alex, Sophia)  
      - Dynatrace Integration with Business Events
      - Real-time Observability Dashboard
      - Service Mesh with Dynamic Service Creation
      Sudo configuration: {{ 'SUCCESS - Passwordless sudo configured' if sudo_test.stdout == 'root' else 'WARNING - May need manual intervention' }}
      Sudo test result: {{ sudo_test.stdout }}
      {% if sudo_test.stdout != 'root' %}
      If deployment fails with permission errors, run manually:
      echo 'dt_training ALL=(ALL) NOPASSWD:ALL' | sudo tee /etc/sudoers.d/dt_training
      {% endif %}

- include_role:
    name: dt-access-token
  vars:
    access_token_var_name: "monaco_cleanup_api_token"
    access_token_scope:
      [
        "slo.read",
        "slo.write",
        "CaptureRequestData",
        "credentialVault.read",
        "credentialVault.write",
        "DataExport",
        "DataPrivacy",
        "ExternalSyntheticIntegration",
        "ReadConfig",
        "WriteConfig",
        "events.ingest",
        "settings.read",
        "settings.write",
        "metrics.ingest",
        "openTelemetryTrace.ingest",
        "logs.ingest"
      ]

# Generate OAuth Token
- block:
    - name: Validate required variables for OAuth
      fail:
        msg: "Required variables (dt_oauth_client_id, dt_oauth_client_secret, dt_oauth_sso_endpoint, dt_environment_url_gen3) are missing."
      when:
        - extra_vars.dt_oauth_client_id is not defined
        - extra_vars.dt_oauth_client_secret is not defined
        - extra_vars.dt_oauth_sso_endpoint is not defined
        - extra_vars.dt_environment_url_gen3 is not defined

    - name: Debug OAuth Configuration
      debug:
        msg: |
          DT_OAUTH_CLIENT_ID: {{ extra_vars.dt_oauth_client_id }}
          DT_OAUTH_CLIENT_SECRET: {{ extra_vars.dt_oauth_client_secret }}
          DT_OAUTH_SSO_ENDPOINT: {{ extra_vars.dt_oauth_sso_endpoint }}
          Resource: {{ extra_vars.dt_oauth_account_urn }}

    - name: Request a DT OAuth Access Token
      ansible.builtin.uri:
        url: "{{ extra_vars.dt_oauth_sso_endpoint }}"
        method: POST
        headers:
          Content-Type: "application/x-www-form-urlencoded"
        body_format: form-urlencoded
        body:
          grant_type: "client_credentials"
          client_id: "{{ extra_vars.dt_oauth_client_id }}"
          client_secret: "{{ extra_vars.dt_oauth_client_secret }}"
          scope: "document:documents:read document:documents:write document:direct-shares:read document:direct-shares:write"
          resource: "{{ extra_vars.dt_oauth_account_urn }}"
      register: auth_response_raw
      failed_when:
        - auth_response_raw.status != 200
      changed_when:
        - auth_response_raw.status == 200

    - name: Set OAuth Access Token
      set_fact:
        dt_oauth_access_token: "{{ auth_response_raw.json.access_token }}"

# Install Biz Flow App
- name: Install Biz Flow App
  ignore_errors: true
  include_role:
    name: dt-platform
    tasks_from: ensure-app
  vars:
    dt_app_id: "{{ item.dt_app_id }}"
    dt_app_version: "{{ item.dt_app_version }}"
    dt_environment_url_gen3: "{{ extra_vars.dt_environment_url_gen3 }}"
    dt_oauth_sso_endpoint: "{{ extra_vars.dt_oauth_sso_endpoint }}"
    dt_oauth_client_id: "{{ extra_vars.dt_oauth_client_id }}"
    dt_oauth_client_secret: "{{ extra_vars.dt_oauth_client_secret }}"
    dt_oauth_account_urn: "{{ extra_vars.dt_oauth_account_urn }}"
  loop:
    - dt_app_id: "dynatrace.biz.flow"
      dt_app_version: "1.20.3"

- include_role:
    name: monaco-v2

- include_role:
    name: monaco-v2
    tasks_from: apply-monaco
    apply:
      environment:
        DT_API_TOKEN: "{{ dynatrace_api_token }}"
        DT_PLATFORM_TENANT_URL: "{{ extra_vars.dt_environment_url_gen3.rstrip('/') }}"
        DT_OAUTH_CLIENT_ID: "{{ extra_vars.dt_oauth_client_id }}"
        DT_OAUTH_CLIENT_SECRET: "{{ extra_vars.dt_oauth_client_secret }}"
        DT_OAUTH_SSO_ENDPOINT: "{{ extra_vars.dt_oauth_sso_endpoint }}"
        INGRESS_DOMAIN: "{{ ingress_domain }}"
  vars:
    monaco_projects_root: "{{ role_path_abs }}/files/monaco"
    monaco_project: "" # selection of projects or all projects under the root path if set empty
    monaco_manifest_path: "{{ role_path_abs }}/files/monaco/manifest.yml"
    monaco_environment:
      DT_API_TOKEN: "{{ dynatrace_api_token }}"
      DT_PLATFORM_TENANT_URL: "{{ extra_vars.dt_environment_url_gen3.rstrip('/') }}"
      DT_OAUTH_CLIENT_ID: "{{ extra_vars.dt_oauth_client_id }}"
      DT_OAUTH_CLIENT_SECRET: "{{ extra_vars.dt_oauth_client_secret }}"
      DT_OAUTH_SSO_ENDPOINT: "{{ extra_vars.dt_oauth_sso_endpoint }}"

# Install Kubernetes infrastructure first
- include_role:
    name: k3s

# Install Helm for Kubernetes package management (using ACE Box official method)
- name: Download Helm binary
  become: true
  become_user: root
  ansible.builtin.get_url:
    url: "https://get.helm.sh/helm-v{{ helm_version | default('3.19.0') }}-linux-amd64.tar.gz"
    dest: "/tmp/helm-v{{ helm_version | default('3.19.0') }}-linux-amd64.tar.gz"
    mode: '0644'

- name: Extract Helm binary
  become: true
  become_user: root
  ansible.builtin.unarchive:
    src: "/tmp/helm-v{{ helm_version | default('3.19.0') }}-linux-amd64.tar.gz"
    dest: "/tmp/"
    remote_src: yes

- name: Move Helm binary to /usr/local/bin
  become: true
  become_user: root
  ansible.builtin.copy:
    src: "/tmp/linux-amd64/helm"
    dest: "/usr/local/bin/helm"
    mode: '0755'
    owner: root
    group: root
    remote_src: true

- name: Remove Helm temp files
  become: true
  become_user: root
  ansible.builtin.file:
    path: "/tmp/helm-v{{ helm_version | default('3.19.0') }}-linux-amd64.tar.gz"
    state: absent
  ignore_errors: yes

- name: Remove Helm extracted directory
  become: true
  become_user: root
  ansible.builtin.file:
    path: "/tmp/linux-amd64"
    state: absent
  ignore_errors: yes

- name: Verify Helm installation
  ansible.builtin.command: helm version --short
  register: helm_version_output

- name: Display Helm version
  debug:
    msg: "Helm installed successfully: {{ helm_version_output.stdout }}"

- include_role:
    name: mattermost

# Install OneAgent after Kubernetes is ready
- include_role:
    name: dt-oneagent-classic

# Partner PowerUp BizObs App - Native EC2 Deployment (No Kubernetes needed)
- include_role:
    name: app-partner-powerup-bizobs

# Apply BizObs Dashboard Configuration (Dashboard runs in K8s)
- name: Add BizObs Dashboard Configuration
  block:
  - set_fact:
      include_dashboard_value_file: "{{ role_path_abs }}/templates/bizobs-dashboard.yml.j2"

  - include_role:
      name: dashboard
      tasks_from: template-values-file

# Deploy dashboard after BizObs configuration is ready (Dashboard runs in K8s)
- include_role:
    name: dashboard

# Wait for dashboard to be ready
- name: Wait for dashboard to be ready
  wait_for:
    host: localhost
    port: 80
    delay: 10
    timeout: 120
  failed_when: false

- name: Check dashboard deployment status
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Pod
    namespace: ace
    label_selectors:
      - "app.kubernetes.io/name=dashboard"
  register: dashboard_pods
  failed_when: false

- name: Display dashboard status
  debug:
    msg: |
      Dashboard deployment status:
      Dashboard URL: https://dashboard.{{ ingress_domain }}
      Dashboard pods: {{ dashboard_pods.resources | length if dashboard_pods.resources is defined else 'Not found' }}
      {% if dashboard_pods.resources is defined and dashboard_pods.resources | length > 0 %}
      Pod status: {{ dashboard_pods.resources[0].status.phase if dashboard_pods.resources[0].status is defined else 'Unknown' }}
      {% endif %}

# Partner PowerUp BizObs App - Start Application and Setup Ingress
- name: Start Application
  ansible.builtin.shell: /bin/sh "{{ bizobs_home | default('/opt/bizobs') }}/Partner-PowerUp-BizObs-App/start-server.sh"
  become: true
  args:
    chdir: "{{ bizobs_home | default('/opt/bizobs') }}/Partner-PowerUp-BizObs-App"
  register: bizobs_start_result
  failed_when: false  # Don't fail immediately, let's check the result

- name: Display BizObs startup result
  debug:
    msg: "BizObs startup result: {{ bizobs_start_result }}"

- name: Wait for BizObs application to be ready
  wait_for:
    port: 8080
    host: localhost
    delay: 10
    timeout: 180  # Increased timeout
  register: port_check
  failed_when: false

- name: Check if BizObs is running via HTTP
  ansible.builtin.uri:
    url: http://localhost:8080/health
    method: GET
  register: health_check
  failed_when: false
  when: port_check is succeeded

- name: Display health check result
  debug:
    msg: "Health check result: {{ health_check }}"
  when: health_check is defined

- name: Create BizObs Service
  kubernetes.core.k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: bizobs-service
        namespace: default
      spec:
        type: ExternalName
        externalName: "{{ ansible_default_ipv4.address }}"
        ports:
        - protocol: TCP
          port: 80
          targetPort: 8080
  when: port_check is succeeded

- name: Create BizObs Ingress
  kubernetes.core.k8s:
    definition:
      apiVersion: networking.k8s.io/v1
      kind: Ingress
      metadata:
        name: bizobs-ingress
        namespace: default
        annotations:
          nginx.ingress.kubernetes.io/rewrite-target: /
          nginx.ingress.kubernetes.io/upstream-vhost: "{{ ansible_default_ipv4.address }}:8080"
      spec:
        ingressClassName: "{{ ingress_class }}"
        rules:
        - host: "bizobs.{{ ingress_domain }}"
          http:
            paths:
            - path: /
              pathType: Prefix
              backend:
                service:
                  name: bizobs-service
                  port:
                    number: 80
  when: port_check is succeeded and health_check is succeeded

# Final deployment status
- name: Display final deployment status
  debug:
    msg: |
      🎉 Partner PowerUp BizObs Deployment Status:
      
      ✅ Dashboard URL: https://dashboard.{{ ingress_domain }}
      ✅ Dashboard Username: dt_training  
      ✅ Dashboard Password: {{ ansible_env.ACE_BOX_DASHBOARD_PASSWORD | default('Check logs for password') }}
      
      ✅ BizObs Application URL: {{ ingress_protocol | default('http') }}://bizobs.{{ ingress_domain }}
      ✅ BizObs Health Check: {{ ingress_protocol | default('http') }}://bizobs.{{ ingress_domain }}/health
      
      📊 Business Analytics Features:
      - Customer Journey Simulation
      - Dynamic Microservices (Step1-Step6)
      - Industry-Specific Templates  
      - Real-time Business Analytics
      - Dynatrace Integration
      
      🔧 Troubleshooting:
      - If dashboard shows 404, wait 2-3 minutes for full deployment
      - Check pod status: kubectl get pods -n ace
      - Check ingress: kubectl get ingress -A
